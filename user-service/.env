# --- DB / Redis ya existentes en tu .env ---
DATABASE_URL=Driver={ODBC Driver 18 for SQL Server};Server=tcp:fitflow-sqlserver,1433;Database=user_db;Uid=sa;Pwd=YourStrong!Passw0rd;Encrypt=no;TrustServerCertificate=yes;Login Timeout=2;

# --- JWT ---
JWT_SECRET=dev-super-secret             # cambia en prod; o usa JWT_PRIVATE_KEY/JWT_PUBLIC_KEY para RSA
JWT_ALG=HS256                           # HS256 para HMAC; si usas RSA pon RS256 y llaves abajo
JWT_EXPIRES_MIN=60

# Si quieres firmar RSA (OPCIONAL, en vez de JWT_SECRET/JWT_ALG=HS256):
# JWT_ALG=RS256
# JWT_PRIVATE_KEY_PATH=/run/secrets/jwt_private.pem
# JWT_PUBLIC_KEY_PATH=/run/secrets/jwt_public.pem

# --- Notificación opcional del token (NO recomendado en prod) ---
JWT_NOTIFY_ON_LOGIN=false  # true para publicar el JWT en el evento UserLoggedIn (para tu demo)

# --- /diag hacia payment (ya lo usamos en resiliencia) ---
PAYMENT_HEALTH_URL=http://payment-service:8002/health
PAYMENT_HTTP_TIMEOUT=2
